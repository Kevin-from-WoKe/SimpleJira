const config = {
  // App identity
  name: "Welcome",
  tagline: "Enter your credentials to access your account.",

  // shadcn preset ID from ui.shadcn.com/create
  // Affects: colors, font, radius
  defaultPreset: "b6FTaxB1H",

  // Show the theme switcher palette icon (disable in production)
  showThemeSwitcher: false,
}

export default config
